version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    container_name: app-dev
    security_opt:
      - "seccomp:unconfined"
    volumes:
      - ./cmd/mlpa:/cmd/mlpa:rw,delegated
      - ./internal:/internal:rw,delegated
      - ./web/template:/web/template:rw,delegated
      - ./lang:/app/lang:rw,delegated
      - /cmd/mlpa/web/static
      - ./.air.toml:/app/.air.toml
    cap_add:
      - SYS_PTRACE
    ports:
      - "2345:2345"
